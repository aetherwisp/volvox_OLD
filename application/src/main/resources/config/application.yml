# ----------------------------------------
# Project specific properties
# ----------------------------------------
aetherwisp:
  volvox:
    application:
      webjars:
        bootstrap: 4.5.0
        jquery: 3.5.1

# ----------------------------------------
# Common Application properties
# ----------------------------------------
server:
  compression:
    enabled: true
  servlet:
    context-path: /volvox
spring:
  mvc:
    favicon:
      enabled: true
  profiles:
    active: product
