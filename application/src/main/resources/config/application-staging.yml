# ----------------------------------------
# Project specific properties
# ----------------------------------------
aetherwisp:
  volvox:
    application:
      version: ${project.version}+${build.number}

# ----------------------------------------
# Common Application properties
# ----------------------------------------
logging:
   config: classpath:log4j2-staging.xml

spring:
  main:
    banner-mode: off
  resources:
    cache:
      cachecontrol:
        max-age: 86400
   